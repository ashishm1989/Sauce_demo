<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="SauceDemo Automation Suite" verbose="2" parallel="false">

    <!-- Attach listeners globally -->
    <listeners>
        <listener class-name="listeners.TestListener"/>
        <listener class-name="listeners.ExtentReportListener"/>
    </listeners>

    <!-- Group 1 : Smoke Tests (fast, must-pass on every build) -->
    <test name="Smoke Tests">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>
        <classes>
            <class name="tests.LoginTest"/>
            <class name="tests.InventoryTest"/>
        </classes>
    </test>

    <!-- Group 2 : End-to-End Checkout Flow -->
    <test name="E2E Checkout Tests">
        <groups>
            <run>
                <include name="e2e"/>
            </run>
        </groups>
        <classes>
            <class name="tests.E2ECheckoutTest"/>
        </classes>
    </test>

</suite>